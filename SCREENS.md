# 워커앱 화면 정본 (SCREENS.md)

웹 `topcare-web/PAGES.md` 와 같은 규약. **새 화면은 여기 정의 블록(왜/답하는 질문/소스/목적 QA)부터 쓴다.**
목적을 못 쓰면 만들지 않는다. QA는 "버튼이 눌리나"가 아니라 아래 **목적 QA 3종**으로 한다.

---

## 네비게이션 구조 (2026-08-06 변경)

**1페이지 1작업.** 하단 탭바(6개)를 없앴다 — 요양원 PoC에서 사용자층(50~70대)이
"지금 뭘 하는 중인지" 잃고 탭 사이로 새는 문제가 나왔다.

```
로그인 → 동의 게이트 → (home)/{역할}  = 큰 버튼 세로 메뉴 (여기서 작업 하나 선택)
                              ↓ push
                        (tabs)/*        = 작업 하나 전체화면 + 상단 큰 [뒤로]
```

- `app/(home)/{role}.tsx` — 역할별 메뉴. `QuickAction`에 `href` 없으면 **표시 안 됨**(죽은 카드 금지).
- `app/(tabs)/_layout.tsx` — 이름만 `(tabs)`인 **Stack**. 폴더명은 href 27곳 보호를 위해 유지.
- 화면은 자체 제목을 그리지 않는다. 제목은 Stack 헤더가 담당.

## 글자·터치 규약

`lib/theme.ts` 가 정본. 화면에서 `fontSize` 숫자를 직접 쓰지 말 것.
**14pt 미만 금지**, 누를 수 있는 것은 **최소 높이 56**.

---

## 화면 정의

### `(tabs)/vitals/measure` — 바이탈 측정

**왜 있나**
간호(조무)사의 오전 라운드는 "한 분 앞에 서서 재고, 적고, 다음 분"이다.
2026-08-06 이전까지 앱에는 조회 화면만 있고 **입력 화면이 없었다** — 현장에서 잰 값을
앱에 넣을 방법 자체가 없었다. 웹 care 화면의 표 형태 일괄입력은 폰에서 칸이 좁아 못 쓴다.

**답하는 질문**
- 지금 이분 값 어디에 적지?
- 오늘 누구까지 쟀지?
- 이 값 이상한가?

**소스**
- `GET /api/vitals` — 입소자 전원 + 최신 실측(미측정은 `measured:false`, 값 전부 null)
- `POST /api/vitals` — `{ measuredDate, entries:[{residentId, temp, spo2, bpSys, bpDia, hr, bloodSugar}] }`
- 혈당은 `bloodSugarTarget`(당뇨 대상자)인 사람에게만 노출 — 스키마 018 규약(격일)

**설계 결정**
- 한 분씩 즉시 저장(웹은 전원 일괄). 라운드 중 앱이 죽어도 앞사람 기록이 남는 쪽이 현장에 맞다.
- 이상치 임계는 서버 `lib/care/vital-alert.ts` 와 **동일**해야 한다. 웹을 바꾸면
  `lib/hooks/useVitals.ts` 의 `alertReasons` 도 같이 바꿀 것 — 어긋나면 앱이 거짓말을 한다.

**목적 QA 3종**
1. **0건** — 입소자 0명인 기관에서 열면 "입소 중인 분이 없습니다" (빈 화면·크래시·목데이터 아님)
2. **1건 왕복** — 한 분에게 체온 36.5 입력 → [저장하고 다음 분] → 웹 care 화면 바이탈에
   그 값과 측정 시각이 뜬다 → 앱 '바이탈 현황'에서도 '미측정'이 사라진다
3. **핵심 질문** — 12명 중 3명 잰 상태에서 화면을 다시 열면 상단이 `3 / 12명 측정`이고,
   '바이탈 현황'에서 못 잰 9명이 위로 올라온다

**정직성 체크**
- 저장 실패 시 다음 분으로 넘어가지 않는다(입력값 유지 + 실패 사유 표시)
- 서버가 조용히 버리는 범위 밖 값(체온 30~45 등)은 저장 전에 앱이 막고 사유를 보여준다
- 이상치 문구는 "확인 권장"까지. 진단하지 않는다(건강보조기기 원칙)

### `(tabs)/vitals` — 바이탈 현황 (조회 전용)

**답하는 질문**: 지금 눈여겨봐야 할 분은 누구인가 / 오늘 아직 안 잰 분은 누구인가
**소스**: `GET /api/vitals`
**정직성**: 미측정을 정상으로 그리지 않는다. 값 없으면 `—` + '미측정' 배지, 정렬에서 위로.

### `(tabs)/observation` — 관찰 기록

**왜 있나**
기존 화면은 8개 임의 카테고리(전반상태/기분/피부...)와 자유 텍스트 빠른선택이었는데,
같은 관찰이 웹의 4도메인 단어카드(신체활동지원/인지관리 및 의사소통/건강 및 간호관리/
기능회복훈련) 체계와 전혀 안 맞아 웹·앱이 서로 다른 어휘로 같은 일을 기록하고 있었다.
2026-09-01 개편으로 버튼 정본을 웹과 동일하게 맞추고, 현장 직접 입력에서 반복되는
표현을 단어카드로 승격하는 학습 루프를 추가했다(현장 어휘가 늘수록 다음번 입력은
버튼 한 번으로 끝난다).

**답하는 질문**
- 이 어르신에게 방금 한 일을 어느 버튼으로 남기지?
- 버튼에 없는 내용은 어디에 적지?
- 내가 자주 직접 쓰는 표현을 매번 다시 타이핑해야 하나?

**소스**
- `GET/POST /api/care/records?recordType=observation` — 기록 저장/조회(기존과 동일)
- `GET /api/care/observation-cards` — 시설 커스텀 단어카드(`lib/hooks/useObservationCards.ts`).
  조회 실패 시 내장 버튼만 보여주고 작은 오류 문구를 띄운다(거짓 빈 상태 금지)
- `POST /api/care/observation-cards` — 새 단어카드 등록(사람이 탭해야만 호출됨)
- `POST /api/care/observation-cards/suggest` — 직접 입력분에서 후보 최대 3개 제안(저장 0,
  초안만 반환). 15자 미만이면 호출 자체를 하지 않는다
- 내장 버튼 정본: `topcare-web/lib/data/observation-buttons.ts` (사본:
  `lib/data/observation-buttons.ts` — 양쪽 동기화 필요)

**설계 결정**
- 선택한 버튼의 autoText와 직접 입력 textarea 값은 별도 상태로 관리하고 저장 시에만
  합친다(문자열에서 역파싱하지 않음) — 재탭으로 개별 해제/제거가 가능해야 하기 때문
- 단어카드 제안·등록은 부가 기능: 실패해도 방금 완료된 기록 저장 결과에는 영향을 주지
  않는다(조용히 접되 콘솔 로그). 단, 제안 카드를 탭해 실제 등록할 때의 성공/실패는
  정직하게 표시한다
- **자동 추가 금지** — 서버가 제안해도 사람이 칩을 탭해야만 `POST .../observation-cards`
  가 호출된다

**목적 QA 3종**
1. **0건** — 커스텀 단어카드가 하나도 없는 신규 기관에서 열면 내장 버튼만 뜨고
   크래시·목데이터 없이 정상 작동한다
2. **1건 왕복** — 버튼 2개 선택 + 직접 입력 20자 작성 → 저장 → 히스토리에 두 버튼의
   autoText와 직접 입력이 이어진 문장으로 뜬다 → 제안 칩이 뜨면 하나를 탭해 등록 →
   해당 도메인 그리드에 즉시 새 버튼으로 나타난다(재조회 없이 같은 화면에서 확인)
3. **핵심 질문** — 도메인 탭을 오가며 각 3개씩 선택하면 탭의 배지 숫자가 선택한 만큼
   올라가고, 저장 후 초기화된다

**정직성 체크**
- 버튼 선택 0개 + 직접 입력 0자면 저장 버튼이 비활성 상태를 유지한다
- 커스텀 카드 조회 실패는 빈 목록으로 위장하지 않고 오류 문구를 보여준다
- LLM 제안 실패는 사용자에게 알리지 않지만(부가 기능), 등록 실패는 반드시 알린다

---

### `(tabs)/programs` + `(tabs)/programs/record` — 오늘 프로그램 / 회차 기록 (vc9, 2026-09-03)

**왜 있나**
사회복지사 홈의 '프로그램' 칩은 `href` 가 없어 **표시조차 되지 않던 죽은 항목**이었다.
#14(2026-09-02 대표)에서 프로그램 입구가 행정업무 → 요양업무 [기록]으로 옮겨졌다 —
프로그램 진행은 케어 행위다. #12 조사에서 확인된 실제 공백은 회차 자체가 아니라
**참여자 개인별 평가**였고, 웹 모달은 생겼지만(1a97380) 프로그램은 프로그램실에서
진행되므로 폰에서 끝나지 않으면 종이 → 웹 이중 입력이 남는다.

**답하는 질문**
- 오늘 무슨 프로그램이 몇 시에 있지?
- 누가 참여 대상이지? / 오늘 누가 왔지?
- 이분 참여도는? / 이 회차 기록 끝났나?

**소스**
- `GET /api/schedule/programs/today` — `planned[]`(오늘 요일 회차 중 **오늘 기록이 없는 것**),
  `recordedToday[]`, `missed[]`(최근 7일 미기록 — 경고까지만), `todo`
  · 회차·그룹은 이 조회가 만들지 않는다(확정은 사람). 저장되는 것은 서버측 오늘 투두 1건뿐.
- `POST /api/schedule/programs/provisions` —
  `{ programId, provisionDate, leadStaffId, participantIds[], participantCount, notes?, participantResults }`
  · ⚠ 키는 **`participantIds`** 다. `participants` 로 보내면 서버가 조용히 0명으로 저장한다.
  · `participantResults` = `{ [residentId]: { participation: '상'|'중'|'하' } }` —
    서버가 참여 체크된 사람만 남기고, 미입력은 저장하지 않는다.
  · 서버가 참여자별 `ServiceProvision`(급여제공기록)까지 연계 생성한다.

**설계 결정**
- **2화면 분리** (목록 / 기록) — 1페이지 1작업. 기록 화면에서 상단 큰 [뒤로]가
  목록으로 돌아가고, 목록의 [뒤로]가 홈으로 간다.
- 기록 화면은 `programId` 만 params 로 받고 회차 정보는 **같은 쿼리 캐시**에서 읽는다
  (params 로 명단을 실어 보내면 화면 사이에서 사실이 갈라진다).
- 참여자는 **전원 선택이 기본**이고 해제할 수 있다 — 그룹 단위로 진행되므로
  "빠진 사람을 지우는" 쪽이 현장 동작이다.
- 참여도는 **미입력을 허용**한다(안 본 것을 '중'으로 채우지 않는다). 같은 등급 재탭 = 해제.
  [전원 상] 1탭은 웹 모달과 같은 편의이고 누른 뒤 개별 조정된다.
- 이미 기록된 회차는 앱에서 다시 만들지 않는다(같은 회차가 두 줄로 남지 않게). 수정은 웹.
- 지난 회차(missed)는 **목록에 경고로만** 띄운다 — 앱이 없던 프로그램을 만들어 주지 않는다.

**진입 경로**
사회복지사 홈 **primary**(주 업무). 시설장·원장 홈은 secondary — "오늘 프로그램이 돌았나"
확인 축이고, 대리 기록도 기록이므로 기록도 가능하다. 간호(조무)사·요양보호사·자원봉사
홈은 제외(기록 주체가 아님 — 죽은 카드를 늘리지 않는다).

**공백 (정직)**
- 만족도·수행도·개인별 메모는 앱에서 받지 않는다(참여도만). 현장 1탭 속도를 우선했고,
  나머지는 웹 모달이 정본이다.
- 회차 수정(PUT)·월간 운영기록지·사회복지사 소견은 앱 미구현(#12 잔여 2단계, 웹).
- 하루 같은 프로그램 2회차는 서버가 첫 기록으로 본다(`programId|날짜` 키) — 앱도 동일.

**목적 QA 3종**
1. **0건** — 프로그램 일정이 없는 기관에서 열면 "오늘 예정된 프로그램이 없습니다" +
   웹에서 만드는 방법 안내. 빈 목록·목데이터·크래시 없음. 조회 실패는 오류 문구 + [다시 시도].
2. **1건 왕복** — 오늘 예정 1건 탭 → 참여자 1명 해제 + 2명 [상] → [기록 저장] →
   목록에서 그 회차가 ✓기록됨으로 바뀌고, 웹 [프로그램] 회차에 참여 인원·참여도가 그대로 뜨며
   참여자들의 급여제공기록(인지활동/정서지원)에도 잡힌다.
3. **핵심 질문** — 그룹 8명 회차에서 [전원 상] 1탭 + 불참 1명 해제 → 총 탭 3회로 저장되고,
   저장 알림이 "참여 7명 / 급여제공기록 7명 반영"을 그대로 알려준다.

**정직성 체크**
- 그룹 명단 중 입소자와 연결하지 못한 이름(`unresolvedMembers`)은 감추지 않고 이름까지 적는다.
- 참여자 0명으로는 저장하지 않는다(빈 회차 생성 금지).
- `serviceProvisionsCreated === 0` 이면 "저장됨"만 쓰지 않고 연계 실패를 알린다.

---

### `components/AttendanceCard` — 근태 카드 (역할 홈 공통, vc9 2026-09-03)

화면이 아니라 **모든 역할 홈 상단의 공통 카드**다. 확정 설계는 웹 리포
`작업일지/2026-09_00_2차PoC_피드백대장.md` "근태(GPS 출퇴근) 확정 설계" 절(2026-09-01 대표).

**왜 있나**
출근 버튼을 없애는 것이 이 기능의 목적이다. 출근 = **앱 실행(포그라운드) + 시설 반경 판정**의
그날 최초 성립 시각, 자동 기록. 사람이 누르는 것은 [퇴근] 하나뿐이다.

**답하는 질문**
- 내 출근이 찍혔나? / 위치가 확인됐나? / 퇴근 어디서 누르지?
- (시설장) 시설 위치가 등록됐나?

**소스**
- `GET  /api/staff/attendance?self=1&date=오늘` — 본인 오늘 기록(0건이면 빈 목록)
- `POST /api/staff/attendance` `{ type:'checkin'|'checkout', lat?, lng?, accuracyM?, mockFlag? }`
  · **시각 정본 = 서버 수신 시각(KST)** — 앱은 시각을 보내지 않는다(폰 시계 조작 가능).
  · 거리 계산·판정·중복 무시는 전부 서버. 같은 날 두 번째 checkin 은 기존 값을 그대로 돌려준다.
- `GET/PUT /api/settings/attendance-geo` — 시설 좌표 등록 상태 / 현장 1탭 등록(관리자만)

**설계 제약 (대표 확정 — 어기지 말 것)**
- **포그라운드 위치만.** 백그라운드 권한·`watchPositionAsync`·TaskManager 금지. 상시 추적 없음.
- **버튼/실행 순간 1회만** 잰다. 폴링·주기 측정 금지. 하루 첫 성공 후 로컬 날짜 기억으로 재전송 없음.
- **좌표는 판정 직후 폐기** — 서버 DB에 남는 것은 거리·정확도·판정(checkMeta)뿐. 앱도 저장 안 함.
- **차단 금지.** 권한 거부·GPS 실패도 출근은 기록하고 "위치 확인 없이 기록됨"으로 적는다.
  권한 강요 화면으로 앱을 막지 않는다(W2 교훈).
- **비콘은 근태·세션을 연장하는 근거가 아니다** — 이 카드는 비콘과 무관하다.
- **개인정보 동의 게이트 통과 후에만 동작한다**(`lib/hooks/useConsentGate`) — 동의 리다이렉트가
  걸리기 전 한 프레임 사이에 위치를 재면 동의 없는 개인정보 처리가 된다.

**설계 결정**
- GPS 오차를 기기가 안 주면 `accuracyM` **키를 보내지 않는다**. 0으로 지어내면 거짓 '확인됨',
  큰 수로 지어내면 어디서 눌러도 '근접'이 된다(서버 판정이 오차만큼 관대해짐).
- 시설 등록은 오차 > 100m(서버 `MAX_ACCURACY_M`)면 왕복 전에 현장에서 바로 재시도 안내.
- 직원 정보가 연결되지 않은 계정은 근태 대상이 아니다 — 빈 카드로 위장하지 않고 그 사실을 적는다.

**목적 QA 3종**
1. **0건** — 시설 위치 미등록 기관에서 앱을 열면 출근은 기록되고 위치는 "위치 미확인"으로 표시된다.
   시설장 홈에는 "등록 전에는 모든 출근이 위치 미확인으로 남습니다" + [시설 위치 등록]이 뜬다.
2. **1건 왕복** — 시설장이 현장에서 [시설 위치 등록] → 직원이 앱을 열면 홈에 "오늘 출근 07:58 ✅
   확인됨" → 웹 근태 화면에 같은 시각·판정이 뜬다 → [퇴근] 1탭 → 퇴근 시각·판정이 함께 뜬다.
3. **핵심 질문** — 위치 권한을 거부한 폰으로 앱을 열어도 출근이 기록되고, 카드에
   "위치 확인 없이 기록됨 — 위치 권한이 없어…"가 적힌다(앱이 막히지 않는다).

**정직성 체크**
- 시각·판정은 서버 응답만 표시한다. 폰 시계로 시각을 그리지 않는다.
- 전송이 실패하면 "출근 기록됨"이라고 쓰지 않는다 — 실패를 적고 [다시 시도]를 준다.
  로컬 날짜 기억도 남기지 않아 다음 포그라운드 진입에서 다시 시도한다.
- 동의 상태 조회가 실패하면 조용히 비워두지 않고 그 이유를 적는다.

---

## 기존 화면 (정의 블록 미작성 — 소급 작성 대상)

`(tabs)/index`(오늘 할 일), `todos`, `care-log`, `health-log`,
`service-log`(비콘 자동기록), `proximity`(비콘 근접), `alerts`.
각 화면 상단 주석에 목적이 적혀 있으나 목적 QA 3종은 아직 없다.

### `(tabs)/workboard` — 공동 작업판 (워커앱 v2.0, 2026-08-21)

**왜 있나**
대표 확인(2026-08-20): 방 배정·담당 선정은 병원식 형식일 뿐, 실업무는 2인 이상 공동 수행이 다수.
개인 담당 목록이 아니라 **근무 중 모두가 같은 판**을 보고 자기가 한 어르신을 체크한다 —
벽 화이트보드의 디지털화. UIUX 철학 정본(20260820) 파일럿 1.

**답하는 질문**
- 지금 이 시간에 누구에게 무엇을 해야 하지?
- 누가 이미 했지? (기록자 이름 표시 — 감시가 아니라 인수인계)
- 이 시간 몇 건 남았지?

**소스**
- `GET /api/care/service-schedules?isActive=true` — 오늘 요일(+매일) 계획이 판이 된다 (시간표가 센서)
- `GET /api/care/service-provisions?date=오늘` — 20초 자동 갱신 = 공동 판 동기화
- `POST /api/care/service-provisions` — 체크(scheduleId 연결, 서버가 배변·목욕·간호 관찰기록 자동 연계)

**설계 결정**
- 정상 = 행 탭 1회, 블록 잔여는 [남은 N건 모두 완료] 1탭 (예외 기반 기록 — 원칙 ④)
- 예외 = [예외] 버튼 → 큰 버튼 3종(거부/절반/이상) → note로 저장. 음성 메모는 후속(STT 결정 대기)
- **배설·목욕 상세 (2026-08-24, 웹 사용성 2순위의 현장 쪽 대응)**: 정상은 그대로 행 탭 1번.
  적을 것이 있을 때만 버튼 이름이 유형별로 바뀐 시트에서 큰 버튼 하나로 고른다 —
  배변 케어 = [배변·이상](대변 있었음 / 설사 / 변비·안 나옴 / 혈변 / 피부 발적·짓무름 / 거부 / 일부만),
  목욕 = [상세·이상](부분목욕만 / 침상목욕 / 피부 발적·상처 / 거부 / 일부만 / 이상 발견).
  '대변 있었음'·'부분목욕만'처럼 이상이 아닌 선택지는 초록 테두리로 구분하고 예외로 세지 않는다.
  고른 값은 제공기록 POST의 `detail` 로 실려 **서버가 만드는 관찰기록(CareRecord)에 담긴다** —
  앱이 기록을 하나 더 만들지 않으므로 같은 사실이 두 줄로 남지 않는다(원칙 ⑦).
  웹 배설관찰·목욕 목록과 서비스제공기록지가 같은 값을 읽는다. 임상 판단은 사람이 고른 것만
  저장하고 추측하지 않는다(탭 1번만 하면 유형은 '서비스 제공' 그대로 — 없는 사실을 만들지 않음).
- **배변 유무 1탭 (vc9, 2026-09-03 — QA P2 제안 채택)**: 위 문단의 "탭 1번만 하면 유형은
  '서비스 제공' 그대로"가 실제로는 **기록은 있는데 내용이 없는 줄**을 만들었다(웹 배설관찰
  목록·기록지에서 무엇을 봤는지 알 수 없음). → 배변 케어 행을 탭하면 저장 **직전에**
  큰 카드 4개([소변] [대변] [둘 다] [없음])를 한 번 띄우고, 고른 값이 `detail.type` 으로
  붙어 저장된다(기존 예외 시트와 같은 detail 경로 — 서버 화이트리스트 키는 `type`,
  값은 웹 배설 라운드 `marks` 와 같은 용어). 총 탭 2회.
  · **저장 전에 묻는 이유**: 저장 후엔 이미 만들어진 관찰기록의 유형을 고칠 API가 없다.
  · **강요 금지**: [건너뛰기]는 detail 없이 저장 = 기존 동작 그대로. [남은 N건 모두 완료]는
    시트를 띄우지 않는다(N개의 시트는 일괄 완료의 목적을 부순다).
  · 양·성상·피부는 여기서 묻지 않는다 — 안 본 것을 '정상'으로 창작하지 않는다(적을 것이
    있으면 [배변·이상] 시트로).
- 기록 1건 원칙: 이미 기록된 행은 기록자 이름 표시, 재기록 안 됨 (탭하면 "이미 OOO님이 기록")
- 판정·차감·관찰기록 연계는 전부 서버 기존 로직 재사용 — 앱은 화면일 뿐 (사실의 주인은 하나)

**공백 (정직)**
- 동시 체크 경합의 서버측 차단(scheduleId+date unique 또는 409) 미구현 — 20초 갱신으로 완화만. 서버 추가 예정
- interval(체위 2시간) 행은 회차 전개 없이 "×N회" 표시만 — 회차별 체크는 후속
- 음성 예외 메모 미구현 (STT 엔진 결정 대기 — 철학 정본 열린 질문 ②)

**진입 경로 (2026-08-23)**
요양보호사 홈 primary + **간호조무사·간호사·시설장(대표)·원장 홈에도 입구**.
시설장 홈에 없어서 대표 계정으로는 작업판을 열 수조차 없었다(현장 확인이 그 홈의 존재 이유인데
"지금 누가 무엇을 했는가"가 빠져 있었음). 시설장이 행을 탭하면 기록자에 본인 이름이 남는다 —
대리 기록도 기록이므로 그대로 남긴다. 사회복지사·자원봉사 홈은 제외(케어 기록 주체가 아님).

**목적 QA 3종**
1. **0건** — 계획 0건 기관: "오늘 계획된 서비스가 없습니다" + 웹에서 만드는 방법 안내 (빈 판 위장 금지)
2. **1건 왕복** — 행 탭 → 웹 /care 서비스기록에 제공기록 초안 생김 + 목욕이면 목욕기록 자동 연계 →
   다른 폰에서 같은 판을 열면 20초 내 그 행이 완료(기록자 이름)로 보인다
3. **핵심 질문** — 10:00 블록 8명 중 예외 1명을 [예외→거부]로 먼저 기록하고 [남은 7건 모두 완료]를
   누르면 8명 전원 기록되고 총 탭 수는 4회다 (800터치→수십터치 KPI의 실측 지점)
