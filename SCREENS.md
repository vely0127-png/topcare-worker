# 워커앱 화면 정본 (SCREENS.md)

웹 `topcare-web/PAGES.md` 와 같은 규약. **새 화면은 여기 정의 블록(왜/답하는 질문/소스/목적 QA)부터 쓴다.**
목적을 못 쓰면 만들지 않는다. QA는 "버튼이 눌리나"가 아니라 아래 **목적 QA 3종**으로 한다.

---

## 네비게이션 구조 (2026-08-06 변경)

**1페이지 1작업.** 하단 탭바(6개)를 없앴다 — 요양원 PoC에서 사용자층(50~70대)이
"지금 뭘 하는 중인지" 잃고 탭 사이로 새는 문제가 나왔다.

```
로그인 → 동의 게이트 → (home)/{역할}  = 큰 버튼 세로 메뉴 (여기서 작업 하나 선택)
                              ↓ push
                        (tabs)/*        = 작업 하나 전체화면 + 상단 큰 [뒤로]
```

- `app/(home)/{role}.tsx` — 역할별 메뉴. `QuickAction`에 `href` 없으면 **표시 안 됨**(죽은 카드 금지).
- `app/(tabs)/_layout.tsx` — 이름만 `(tabs)`인 **Stack**. 폴더명은 href 27곳 보호를 위해 유지.
- 화면은 자체 제목을 그리지 않는다. 제목은 Stack 헤더가 담당.

## 글자·터치 규약

`lib/theme.ts` 가 정본. 화면에서 `fontSize` 숫자를 직접 쓰지 말 것.
**14pt 미만 금지**, 누를 수 있는 것은 **최소 높이 56**.

---

## 화면 정의

### `(tabs)/vitals/measure` — 바이탈 측정

**왜 있나**
간호(조무)사의 오전 라운드는 "한 분 앞에 서서 재고, 적고, 다음 분"이다.
2026-08-06 이전까지 앱에는 조회 화면만 있고 **입력 화면이 없었다** — 현장에서 잰 값을
앱에 넣을 방법 자체가 없었다. 웹 care 화면의 표 형태 일괄입력은 폰에서 칸이 좁아 못 쓴다.

**답하는 질문**
- 지금 이분 값 어디에 적지?
- 오늘 누구까지 쟀지?
- 이 값 이상한가?

**소스**
- `GET /api/vitals` — 입소자 전원 + 최신 실측(미측정은 `measured:false`, 값 전부 null)
- `POST /api/vitals` — `{ measuredDate, entries:[{residentId, temp, spo2, bpSys, bpDia, hr, bloodSugar}] }`
- 혈당은 `bloodSugarTarget`(당뇨 대상자)인 사람에게만 노출 — 스키마 018 규약(격일)

**설계 결정**
- 한 분씩 즉시 저장(웹은 전원 일괄). 라운드 중 앱이 죽어도 앞사람 기록이 남는 쪽이 현장에 맞다.
- 이상치 임계는 서버 `lib/care/vital-alert.ts` 와 **동일**해야 한다. 웹을 바꾸면
  `lib/hooks/useVitals.ts` 의 `alertReasons` 도 같이 바꿀 것 — 어긋나면 앱이 거짓말을 한다.

**목적 QA 3종**
1. **0건** — 입소자 0명인 기관에서 열면 "입소 중인 분이 없습니다" (빈 화면·크래시·목데이터 아님)
2. **1건 왕복** — 한 분에게 체온 36.5 입력 → [저장하고 다음 분] → 웹 care 화면 바이탈에
   그 값과 측정 시각이 뜬다 → 앱 '바이탈 현황'에서도 '미측정'이 사라진다
3. **핵심 질문** — 12명 중 3명 잰 상태에서 화면을 다시 열면 상단이 `3 / 12명 측정`이고,
   '바이탈 현황'에서 못 잰 9명이 위로 올라온다

**정직성 체크**
- 저장 실패 시 다음 분으로 넘어가지 않는다(입력값 유지 + 실패 사유 표시)
- 서버가 조용히 버리는 범위 밖 값(체온 30~45 등)은 저장 전에 앱이 막고 사유를 보여준다
- 이상치 문구는 "확인 권장"까지. 진단하지 않는다(건강보조기기 원칙)

### `(tabs)/vitals` — 바이탈 현황 (조회 전용)

**답하는 질문**: 지금 눈여겨봐야 할 분은 누구인가 / 오늘 아직 안 잰 분은 누구인가
**소스**: `GET /api/vitals`
**정직성**: 미측정을 정상으로 그리지 않는다. 값 없으면 `—` + '미측정' 배지, 정렬에서 위로.

---

## 기존 화면 (정의 블록 미작성 — 소급 작성 대상)

`(tabs)/index`(오늘 할 일), `todos`, `care-log`, `health-log`, `observation`,
`service-log`(비콘 자동기록), `proximity`(비콘 근접), `alerts`.
각 화면 상단 주석에 목적이 적혀 있으나 목적 QA 3종은 아직 없다.

### `(tabs)/workboard` — 공동 작업판 (워커앱 v2.0, 2026-08-21)

**왜 있나**
대표 확인(2026-08-20): 방 배정·담당 선정은 병원식 형식일 뿐, 실업무는 2인 이상 공동 수행이 다수.
개인 담당 목록이 아니라 **근무 중 모두가 같은 판**을 보고 자기가 한 어르신을 체크한다 —
벽 화이트보드의 디지털화. UIUX 철학 정본(20260820) 파일럿 1.

**답하는 질문**
- 지금 이 시간에 누구에게 무엇을 해야 하지?
- 누가 이미 했지? (기록자 이름 표시 — 감시가 아니라 인수인계)
- 이 시간 몇 건 남았지?

**소스**
- `GET /api/care/service-schedules?isActive=true` — 오늘 요일(+매일) 계획이 판이 된다 (시간표가 센서)
- `GET /api/care/service-provisions?date=오늘` — 20초 자동 갱신 = 공동 판 동기화
- `POST /api/care/service-provisions` — 체크(scheduleId 연결, 서버가 배변·목욕·간호 관찰기록 자동 연계)

**설계 결정**
- 정상 = 행 탭 1회, 블록 잔여는 [남은 N건 모두 완료] 1탭 (예외 기반 기록 — 원칙 ④)
- 예외 = [예외] 버튼 → 큰 버튼 3종(거부/절반/이상) → note로 저장. 음성 메모는 후속(STT 결정 대기)
- **배설·목욕 상세 (2026-08-24, 웹 사용성 2순위의 현장 쪽 대응)**: 정상은 그대로 행 탭 1번.
  적을 것이 있을 때만 버튼 이름이 유형별로 바뀐 시트에서 큰 버튼 하나로 고른다 —
  배변 케어 = [배변·이상](대변 있었음 / 설사 / 변비·안 나옴 / 혈변 / 피부 발적·짓무름 / 거부 / 일부만),
  목욕 = [상세·이상](부분목욕만 / 침상목욕 / 피부 발적·상처 / 거부 / 일부만 / 이상 발견).
  '대변 있었음'·'부분목욕만'처럼 이상이 아닌 선택지는 초록 테두리로 구분하고 예외로 세지 않는다.
  고른 값은 제공기록 POST의 `detail` 로 실려 **서버가 만드는 관찰기록(CareRecord)에 담긴다** —
  앱이 기록을 하나 더 만들지 않으므로 같은 사실이 두 줄로 남지 않는다(원칙 ⑦).
  웹 배설관찰·목욕 목록과 서비스제공기록지가 같은 값을 읽는다. 임상 판단은 사람이 고른 것만
  저장하고 추측하지 않는다(탭 1번만 하면 유형은 '서비스 제공' 그대로 — 없는 사실을 만들지 않음).
- 기록 1건 원칙: 이미 기록된 행은 기록자 이름 표시, 재기록 안 됨 (탭하면 "이미 OOO님이 기록")
- 판정·차감·관찰기록 연계는 전부 서버 기존 로직 재사용 — 앱은 화면일 뿐 (사실의 주인은 하나)

**공백 (정직)**
- 동시 체크 경합의 서버측 차단(scheduleId+date unique 또는 409) 미구현 — 20초 갱신으로 완화만. 서버 추가 예정
- interval(체위 2시간) 행은 회차 전개 없이 "×N회" 표시만 — 회차별 체크는 후속
- 음성 예외 메모 미구현 (STT 엔진 결정 대기 — 철학 정본 열린 질문 ②)

**진입 경로 (2026-08-23)**
요양보호사 홈 primary + **간호조무사·간호사·시설장(대표)·원장 홈에도 입구**.
시설장 홈에 없어서 대표 계정으로는 작업판을 열 수조차 없었다(현장 확인이 그 홈의 존재 이유인데
"지금 누가 무엇을 했는가"가 빠져 있었음). 시설장이 행을 탭하면 기록자에 본인 이름이 남는다 —
대리 기록도 기록이므로 그대로 남긴다. 사회복지사·자원봉사 홈은 제외(케어 기록 주체가 아님).

**목적 QA 3종**
1. **0건** — 계획 0건 기관: "오늘 계획된 서비스가 없습니다" + 웹에서 만드는 방법 안내 (빈 판 위장 금지)
2. **1건 왕복** — 행 탭 → 웹 /care 서비스기록에 제공기록 초안 생김 + 목욕이면 목욕기록 자동 연계 →
   다른 폰에서 같은 판을 열면 20초 내 그 행이 완료(기록자 이름)로 보인다
3. **핵심 질문** — 10:00 블록 8명 중 예외 1명을 [예외→거부]로 먼저 기록하고 [남은 7건 모두 완료]를
   누르면 8명 전원 기록되고 총 탭 수는 4회다 (800터치→수십터치 KPI의 실측 지점)
