# deps
node_modules/

# expo
.expo/
dist/
web-build/

# env / secrets
.env
.env.local
.env*.local

# native
# android/는 2026-08-03부터 커밋 대상 — Android Studio에서 직접 여는 프로젝트.
# (app.json 변경 시 npx expo prebuild --platform android 재실행으로 동기화)
ios/
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/release/

# misc
*.log
# gradle 백그라운드 빌드 로그(MCP 45초 제한 우회용 폴링 파일) — 커밋 대상 아님
build-log.txt
.DS_Store

# 웹 QA export 산출물·로그
dist/
webexport-log.txt
